<van-notice-bar
  text="提示：输入视频链接，如：https://v.douyin.com/e5KPUNB/"
/>
<view class="container">
  <van-steps steps="{{ steps }}" direction="vertical" active="{{2}}" />
  <view class="top">
    <van-cell-group>
      <van-field value="{{ url }}" center clearable  placeholder="请输入视频链接" bind:change="write"
        use-button-slot>
        <van-button slot="button"  type="primary" bind:click="do">
          提取
        </van-button>
      </van-field>
    </van-cell-group>
  </view>
  <view class="tip"></view>
<van-toast id="van-toast" />
</view>